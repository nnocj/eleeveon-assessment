"use client"; import type {IdentityActivityEvent} from "../types"; import {ActivityOutcomeBadge} from "./ActivityOutcomeBadge"; export function ActivityTimeline({events}:{events:readonly IdentityActivityEvent[]}){return <ol style={{listStyle:"none",padding:0,margin:0,display:"grid",gap:10}}>{[...events].sort((a,b)=>b.occurredAt-a.occurredAt).map(e=><li key={e.id} style={{borderLeft:"3px solid var(--primary,#2563eb)",paddingLeft:10}}><div style={{display:"flex",justifyContent:"space-between",gap:8}}><strong>{e.action||e.purpose.replaceAll("_"," ")}</strong><ActivityOutcomeBadge outcome={e.outcome}/></div><small>{new Date(e.occurredAt).toLocaleString()}</small></li>)}</ol>}; export default ActivityTimeline;