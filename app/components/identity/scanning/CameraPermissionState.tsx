"use client"; export type CameraPermissionStatus="idle"|"requesting"|"granted"|"denied"|"unsupported"; export function CameraPermissionState({status,onRequest}:{status:CameraPermissionStatus;onRequest?:()=>void}){const m={idle:"Camera permission is required.",requesting:"Requesting camera access…",granted:"Camera ready.",denied:"Camera access was denied.",unsupported:"Camera scanning is not supported."};return <div style={{padding:10,borderRadius:10,background:"var(--muted,rgba(148,163,184,.1))"}}><small>{m[status]}</small>{(status==="idle"||status==="denied")&&onRequest&&<button type="button" onClick={onRequest} style={{marginLeft:8}}>Allow camera</button>}</div>}; export default CameraPermissionState;