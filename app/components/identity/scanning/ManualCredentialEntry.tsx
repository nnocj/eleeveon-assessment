"use client"; import {useState,type FormEvent} from "react"; export function ManualCredentialEntry({onValue,disabled=false}:{onValue:(v:string)=>void|Promise<void>;disabled?:boolean}){const[v,setV]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();if(!v.trim())return;await onValue(v.trim());setV("")};return <form onSubmit={submit} style={{display:"grid",gap:8}}><label>Credential reference<input value={v} onChange={e=>setV(e.target.value)} disabled={disabled} placeholder="Card number, ID or reference"/></label><button disabled={disabled||!v.trim()}>Verify credential</button></form>}; export default ManualCredentialEntry;