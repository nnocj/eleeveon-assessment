"use client"; import {useState,type FormEvent} from "react"; export function BarcodeScanner({onValue,disabled=false}:{onValue:(v:string)=>void|Promise<void>;disabled?:boolean}){const[v,setV]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();if(!v.trim())return;await onValue(v.trim());setV("")};return <form onSubmit={submit} style={{display:"flex",gap:8}}><input autoFocus value={v} onChange={e=>setV(e.target.value)} disabled={disabled} placeholder="Scan barcode or enter value" style={{flex:1}}/><button disabled={disabled||!v.trim()}>Verify</button></form>}; export default BarcodeScanner;