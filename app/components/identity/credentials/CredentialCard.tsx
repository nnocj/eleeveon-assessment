"use client"; import type {IdentityCredential,IdentitySubjectOption} from "../types"; import {IdentityStatusBadge} from "../shared"; import {CredentialTypeBadge} from "./CredentialTypeBadge"; export function CredentialCard({credential,subject,onOpen}:{credential:IdentityCredential;subject?:IdentitySubjectOption|null;onOpen?:(c:IdentityCredential)=>void}){return <button type="button" onClick={()=>onOpen?.(credential)} style={{width:"100%",display:"grid",gap:8,textAlign:"left",padding:12,border:"1px solid var(--border,rgba(15,23,42,.1))",borderRadius:14,background:"var(--background,#fff)",color:"inherit"}}><div style={{display:"flex",justifyContent:"space-between",gap:8}}><strong>{credential.label||subject?.name||"Identity credential"}</strong><IdentityStatusBadge status={credential.status}/></div><div style={{display:"flex",gap:6,flexWrap:"wrap"}}><CredentialTypeBadge type={credential.credentialType}/>{credential.serialNumber&&<small>Serial: {credential.serialNumber}</small>}</div><small>{subject?`${subject.subjectType} · ${subject.reference||subject.id}`:`${credential.subjectType} · ${credential.subjectId}`}</small></button>}; export default CredentialCard;