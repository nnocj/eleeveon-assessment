"use client"; import type {IdentityCredentialEvent} from "../types"; import {IdentityPurposeBadge} from "../shared"; export function CredentialHistory({events}:{events:readonly IdentityCredentialEvent[]}){return <ol style={{display:"grid",gap:10,listStyle:"none",padding:0}}>{[...events].sort((a,b)=>b.occurredAt-a.occurredAt).map(e=><li key={e.id} style={{padding:10,borderLeft:"3px solid var(--primary,#2563eb)"}}><div style={{display:"flex",justifyContent:"space-between",gap:8}}><strong>{e.eventType.replaceAll("_"," ")}</strong><time>{new Date(e.occurredAt).toLocaleString()}</time></div>{e.purpose&&<IdentityPurposeBadge purpose={e.purpose}/>} {e.note&&<small>{e.note}</small>}</li>)}{!events.length&&<small>No credential history.</small>}</ol>}; export default CredentialHistory;