"use client";
import type { JourneyEventView } from "../advanced-shared";
import { AdvancedIdentityStyles, EmptyState, formatIdentityDate, labelize } from "../advanced-shared";
export function JourneyEventTimeline({events,primaryColor,onSelect}:{events:readonly JourneyEventView[];primaryColor?:string;onSelect?:(v:JourneyEventView)=>void}) {const rows=[...events].sort((a,b)=>new Date(a.occurredAt).getTime()-new Date(b.occurredAt).getTime());return <section className="journey-timeline"><AdvancedIdentityStyles primaryColor={primaryColor}/><style>{css}</style>{rows.length?rows.map(e=><button type="button" key={e.id} onClick={()=>onSelect?.(e)}><i/><span><strong>{e.studentName||e.studentId}</strong><small>{labelize(e.eventType)}{e.stopName?` · ${e.stopName}`:""}</small><em>{formatIdentityDate(e.occurredAt)}</em></span></button>):<EmptyState title="No journey events" message="Boarding, drop-off and movement events will appear here."/ >}</section>}
const css=`.journey-timeline{display:grid;gap:0}.journey-timeline>button{position:relative;border:0;background:transparent;color:var(--text-color,#172033);display:grid;grid-template-columns:18px 1fr;gap:8px;text-align:left;padding:8px;cursor:pointer}.journey-timeline>button:before{content:"";position:absolute;left:16px;top:0;bottom:0;width:2px;background:rgba(148,163,184,.18)}.journey-timeline>button i{z-index:1;width:10px;height:10px;border-radius:50%;background:var(--ai-primary);margin-top:3px}.journey-timeline>button span{display:grid}.journey-timeline strong{font-size:11px}.journey-timeline small,.journey-timeline em{font-size:9px;opacity:.6;font-style:normal}`;
export default JourneyEventTimeline;
