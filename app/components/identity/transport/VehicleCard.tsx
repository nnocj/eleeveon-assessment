"use client";
import type { VehicleView } from "../advanced-shared";
import { AdvancedIdentityStyles, StatusPill } from "../advanced-shared";
export function VehicleCard({vehicle,primaryColor,onOpen,onStartJourney}:{vehicle:VehicleView;primaryColor?:string;onOpen?:(v:VehicleView)=>void;onStartJourney?:(v:VehicleView)=>void}) {return <article className="ai-card"><AdvancedIdentityStyles primaryColor={primaryColor}/><header style={{display:"flex",gap:9,alignItems:"center"}}><span style={{width:40,height:40,borderRadius:13,display:"grid",placeItems:"center",background:"color-mix(in srgb,var(--ai-primary) 13%,transparent)",fontSize:20}}>▰</span><span style={{display:"grid",flex:1}}><strong>{vehicle.name}</strong><small style={{fontSize:9,opacity:.6}}>{vehicle.registrationNumber} · {vehicle.vehicleType||"vehicle"}</small></span><StatusPill status={vehicle.status||vehicle.active===false?"inactive":"active"}/></header><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:6,marginTop:11,fontSize:9}}><span><b>Capacity</b><br/>{vehicle.capacity||0}</span><span><b>On board</b><br/>{vehicle.occupantCount||0}</span><span><b>Driver</b><br/>{vehicle.driverName||"—"}</span></div><div className="ai-actions" style={{marginTop:11}}>{onOpen?<button onClick={()=>onOpen(vehicle)}>Details</button>:null}{onStartJourney?<button className="primary" onClick={()=>onStartJourney(vehicle)}>Start journey</button>:null}</div></article>}
export default VehicleCard;
