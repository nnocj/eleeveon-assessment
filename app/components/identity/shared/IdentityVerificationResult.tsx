"use client"; import type {IdentityScanResult} from "../types"; import {IdentityVerificationBadge} from "./IdentityVerificationBadge"; export function IdentityVerificationResult({result}:{result:IdentityScanResult}){return <article style={{display:"grid",gap:8,padding:12,border:"1px solid var(--border,rgba(15,23,42,.1))",borderRadius:12}}><div style={{display:"flex",justifyContent:"space-between",gap:8}}><strong>Verification result</strong><IdentityVerificationBadge status={result.verificationStatus??"pending"}/></div><small>{result.failureMessage||(result.subjectId?`Matched ${result.subjectType??"subject"} ${result.subjectId}`:"No subject matched yet.")}</small>{typeof result.confidenceScore==="number"&&<small>Confidence: {Math.round(result.confidenceScore*100)}%</small>}</article>}; export default IdentityVerificationResult;
