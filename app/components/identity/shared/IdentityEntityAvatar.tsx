"use client";
export function IdentityEntityAvatar({name,imageUrl,size=40}:{name:string;imageUrl?:string|null;size?:number}){const initials=name.trim().split(/\s+/).slice(0,2).map(p=>p[0]?.toUpperCase()).join("")||"?";return <span aria-label={name} style={{width:size,height:size,borderRadius:"50%",display:"inline-grid",placeItems:"center",overflow:"hidden",flex:"0 0 auto",background:"var(--primary-soft,rgba(37,99,235,.1))",color:"var(--primary,#2563eb)",fontWeight:800}}>{imageUrl?<img src={imageUrl} alt={name} width={size} height={size} style={{width:"100%",height:"100%",objectFit:"cover"}}/>:initials}</span>}; export default IdentityEntityAvatar;
