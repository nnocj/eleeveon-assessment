"use client"; import type {IdentityCredentialStatus} from "../types"; const L:Record<IdentityCredentialStatus,string>={pending:"Pending",active:"Active",suspended:"Suspended",expired:"Expired",revoked:"Revoked",replaced:"Replaced"}; export function IdentityStatusBadge({status}:{status:IdentityCredentialStatus}){return <span style={{display:"inline-flex",padding:"3px 8px",borderRadius:999,fontSize:12,fontWeight:700,background:status==="active"?"var(--success-soft,rgba(22,163,74,.1))":"var(--muted,rgba(148,163,184,.14))"}}>{L[status]}</span>}; export default IdentityStatusBadge;
