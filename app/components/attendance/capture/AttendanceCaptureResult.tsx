"use client";
import { AttendanceStatusBadge, AttendanceVerificationBadge, CaptureMethodBadge } from "../shared";
import { DuplicateCaptureWarning } from "./DuplicateCaptureWarning";
import type { AttendanceCaptureDisplayResult } from "./types";
export interface AttendanceCaptureResultProps extends AttendanceCaptureDisplayResult { onClear?:()=>void; }
export function AttendanceCaptureResult({event,person,duplicate,message,success,onClear}:AttendanceCaptureResultProps){if(!event&&!message)return null;return <section style={{display:"grid",gap:9,padding:12,borderRadius:13,border:"1px solid var(--border,rgba(15,23,42,.11))",background:"var(--background,#fff)"}}><div style={{display:"flex",gap:9,alignItems:"center"}}><div style={{width:38,height:38,borderRadius:999,display:"grid",placeItems:"center",background:success===false?"var(--danger-soft,rgba(220,38,38,.1))":"var(--success-soft,rgba(22,163,74,.11))",fontWeight:900}}>{success===false?"×":"✓"}</div><div style={{flex:1,minWidth:0}}><strong style={{fontSize:13}}>{person?.name??(success===false?"Capture failed":"Attendance captured")}</strong><div style={{fontSize:11,color:"var(--muted-foreground,#64748b)"}}>{message??person?.subtitle??person?.reference}</div></div>{onClear&&<button type="button" onClick={onClear}>Clear</button>}</div>{event&&<div style={{display:"flex",gap:6,flexWrap:"wrap"}}>{event.attendanceStatus&&<AttendanceStatusBadge status={event.attendanceStatus}/>}<CaptureMethodBadge method={event.captureMethod}/><AttendanceVerificationBadge status={event.verificationStatus}/><span style={{fontSize:10,color:"var(--muted-foreground,#64748b)"}}>{new Date(event.capturedAt).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}</span></div>}{duplicate&&<DuplicateCaptureWarning duplicate={duplicate}/>}</section>}
export default AttendanceCaptureResult;
