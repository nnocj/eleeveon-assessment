"use client";
import type { StudentAttendanceSummary as Summary } from "../../../lib/attendance";
export function StudentAttendanceSummary({summary}:{summary?:Summary|null}){if(!summary)return <div style={{fontSize:11,color:"var(--muted-foreground,#64748b)"}}>No summary available.</div>;const items=[["Present",summary.daysPresent],["Absent",summary.daysAbsent],["Late",summary.timesLate??0],["Opened",summary.daysOpened]] as const;return <section style={{display:"grid",gap:9}}><div style={{display:"flex",alignItems:"end",gap:7}}><strong style={{fontSize:24}}>{summary.attendancePercent.toFixed(1)}%</strong><span style={{fontSize:11,color:"var(--muted-foreground,#64748b)"}}>attendance</span></div><div style={{display:"grid",gridTemplateColumns:"repeat(4,minmax(0,1fr))",gap:6}}>{items.map(([l,v])=><div key={l} style={{padding:7,borderRadius:9,background:"var(--muted,rgba(148,163,184,.12))"}}><strong style={{display:"block",fontSize:13}}>{v}</strong><span style={{fontSize:9}}>{l}</span></div>)}</div></section>}
export default StudentAttendanceSummary;
