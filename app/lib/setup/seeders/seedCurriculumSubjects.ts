import type { SeedContext, SeedDefinition } from "../types";import { mustId,upsertSeed } from "../seedUtils";export async function seedCurriculumSubjects(ctx:SeedContext,defs:SeedDefinition[]){for(const d of defs)await upsertSeed(ctx,"curriculumSubjects",d,{...d,key:undefined,curriculumId:mustId(ctx,"curriculums",d.curriculumKey),subjectId:mustId(ctx,"subjects",d.subjectKey),pathwayId:d.pathwayKey?mustId(ctx,"curriculumPathways",d.pathwayKey):undefined});}