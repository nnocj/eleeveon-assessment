import type { SeedContext, SeedDefinition } from "../types";import { mustId,upsertSeed } from "../seedUtils";export async function seedSubjectPrerequisites(ctx:SeedContext,defs:SeedDefinition[]){for(const d of defs)await upsertSeed(ctx,"subjectPrerequisites",d,{...d,key:undefined,curriculumSubjectId:mustId(ctx,"curriculumSubjects",d.curriculumSubjectKey),prerequisiteSubjectId:mustId(ctx,"subjects",d.prerequisiteSubjectKey)});}