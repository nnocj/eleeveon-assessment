import type { SeedContext, SeedDefinition } from "../types";import { mustId,upsertSeed } from "../seedUtils";export async function seedCurriculums(ctx:SeedContext,defs:SeedDefinition[]){for(const d of defs)await upsertSeed(ctx,"curriculums",d,{...d,key:undefined,academicStructureId:mustId(ctx,"academicStructures",d.academicStructureKey),effectiveFrom:d.effectiveFrom??ctx.academicYearStart,effectiveTo:d.effectiveTo??ctx.academicYearEnd});}