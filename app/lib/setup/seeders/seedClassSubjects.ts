import type { SeedContext, SeedDefinition } from "../types";import { mustId,upsertSeed } from "../seedUtils";export async function seedClassSubjects(ctx:SeedContext,defs:SeedDefinition[]){for(const d of defs)await upsertSeed(ctx,"classSubjects",d,{...d,key:undefined,classId:mustId(ctx,"classes",d.classKey),subjectId:mustId(ctx,"subjects",d.subjectKey),curriculumSubjectId:mustId(ctx,"curriculumSubjects",d.curriculumSubjectKey),academicStructureId:mustId(ctx,"academicStructures",d.academicStructureKey)});}