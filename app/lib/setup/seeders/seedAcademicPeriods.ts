import type { SeedContext, SeedDefinition } from "../types";import { mustId,upsertSeed } from "../seedUtils";export async function seedAcademicPeriods(ctx:SeedContext,defs:SeedDefinition[]){for(const d of defs)await upsertSeed(ctx,"academicPeriods",d,{...d,key:undefined,academicStructureId:mustId(ctx,"academicStructures",d.academicStructureKey),startDate:d.startDate??ctx.academicYearStart,endDate:d.endDate??ctx.academicYearEnd});}