import type { SeedBranchOptions, SeedEntityName } from "./types";import { getSetupTemplate } from "./templates";
export async function inspectBranchSetup(options:SeedBranchOptions){const t=getSetupTemplate(options.templateCode);const result:Partial<Record<SeedEntityName,{expected:number;found:number;missing:string[]}>>={};for(const table of Object.keys(t) as SeedEntityName[]){const defs=(t as unknown as Record<string,unknown>)[table];if(!Array.isArray(defs))continue;const missing:string[]=[];let found=0;for(const d of defs){const row=await options.store.findOne(table,{accountId:options.accountId,schoolId:options.schoolId,branchId:options.branchId,setupTemplateCode:t.code,setupSeedKey:`${table}:${d.key}`,isDeleted:false});row?found++:missing.push(d.key);}result[table]={expected:defs.length,found,missing};}return result;}
